<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/add_note" android:title="Add Notes"
        android:orderInCategory="100" android:showAsAction="always" android:onClick="AddNote" />
</menu>
